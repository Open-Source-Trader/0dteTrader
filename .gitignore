# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local

# iOS
apps/ios/build/
apps/ios/DerivedData/
apps/ios/0dteTrader.xcodeproj/
apps/ios/0dteTrader/App/GeneratedEnvironment.swift
xcuserdata/
*.ipa
*.dSYM.zip

# Prisma
apps/api/prisma/*.db

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Playwright MCP session artifacts
.playwright-mcp/

# Codex audit scan dumps
.codex-audit/

# Internal working notes (sprint plans, lessons) — local only
tasks/

# Claude Code: the shared allowlist is committed, per-machine overrides are not
.claude/settings.local.json

# Pi coding agent artifacts
.pi-subagents

# Throwaway repro/debug specs. These get written straight into src/ while
# chasing a bug and are trivially forgotten — a committed one ships as a real
# test and breaks format:check. Name scratch work with these prefixes.
**/zz-*.spec.ts
**/__tmp*
