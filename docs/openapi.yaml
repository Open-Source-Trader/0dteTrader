openapi: 3.0.3
info:
  title: 0dteTrader API
  version: 0.1.0
servers:
  - url: /v1
paths:
  /auth/register:
    post:
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Credentials' }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/AuthTokens' } } } }
        '409': { description: Email already registered }
  /auth/login:
    post:
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Credentials' }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/AuthTokens' } } } }
        '401': { description: Invalid credentials }
  /auth/refresh:
    post:
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties: { refreshToken: { type: string } }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/AuthTokens' } } } }
        '401': { description: Invalid or expired refresh token }
  /auth/logout:
    post:
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties: { refreshToken: { type: string } }
      responses:
        '204': { description: Logged out }
  /me:
    get:
      tags: [user]
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/Me' } } } }
    patch:
      tags: [user]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tradingMode]
              properties:
                tradingMode: { type: string, enum: [live, practice] }
      responses:
        '200': { description: Updated, content: { application/json: { schema: { $ref: '#/components/schemas/Me' } } } }
  /me/webull-credentials:
    put:
      tags: [user]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/WebullCredentialsInput' }
      responses:
        '200': { description: Saved }
    delete:
      tags: [user]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: environment, in: query, schema: { type: string, enum: [live, practice], default: live } }
      responses:
        '204': { description: Deleted }
  /me/webull-session/refresh:
    post:
      tags: [user]
      security: [{ bearerAuth: [] }]
      responses:
        '200':
          description: Fresh Webull access token minted for the caller's current trading mode
          content:
            application/json:
              schema:
                type: object
                required: [refreshed, environment]
                properties:
                  refreshed: { type: boolean, enum: [true] }
                  environment: { type: string, enum: [live, practice] }
  /market/quote:
    get:
      tags: [market]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: symbol, in: query, required: true, schema: { type: string } }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/Quote' } } } }
  /market/candles:
    get:
      tags: [market]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: symbol, in: query, required: true, schema: { type: string } }
        - { name: interval, in: query, required: true, schema: { type: string, enum: [1m, 5m, 15m, 1h, 1d] } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
      responses:
        '200': { description: OK, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Candle' } } } } }
  /market/options-chain:
    get:
      tags: [market]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: symbol, in: query, required: true, schema: { type: string } }
        - { name: expiration, in: query, schema: { type: string, format: date } }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/OptionsChain' } } } }
  /market/gex:
    get:
      tags: [market]
      security: [{ bearerAuth: [] }]
      description: Dealer GEX/DEX level structure + premium heat map for one underlying (Tradier-backed). OI is the static intraday baseline; prices/Greeks refresh per request.
      parameters:
        - { name: symbol, in: query, required: true, schema: { type: string } }
        - { name: expiration, in: query, schema: { type: string, format: date, description: 'Defaults to 0DTE when today is an expiration, else the nearest' } }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/GexLevels' } } } }
        '400': { description: Unknown expiration or invalid symbol }
        '503': { description: Tradier not configured or unreachable (and no last-good cache) }
  /orders/preview:
    post:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/OrderRequest' }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/OrderPreview' } } } }
  /orders:
    post:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: Idempotency-Key, in: header, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/OrderRequest' }
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/OrderResult' } } } }
        '403': { description: Trading disabled (kill switch) }
        '409': { description: An order with this idempotency key is already being placed }
    get:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: OK, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/OrderResult' } } } } }
  /orders/history:
    get:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      description: Every order, newest first, with per-fill realized P/L (average-cost, multiplier-aware) and the net realized total.
      responses:
        '200': { description: OK, content: { application/json: { schema: { $ref: '#/components/schemas/TradeHistory' } } } }
  /orders/{orderId}:
    delete:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string } }
      responses:
        '204': { description: Cancelled }
  /positions:
    get:
      tags: [trading]
      security: [{ bearerAuth: [] }]
      responses:
        '200': { description: OK, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Position' } } } } }
components:
  securitySchemes:
    bearerAuth: { type: http, scheme: bearer, bearerFormat: JWT }
  schemas:
    Credentials:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string, minLength: 8 }
    AuthTokens:
      type: object
      properties:
        accessToken: { type: string }
        refreshToken: { type: string }
        expiresIn: { type: integer }
    Me:
      type: object
      properties:
        id: { type: string }
        email: { type: string }
        tradingDisabled: { type: boolean }
        tradingMode: { type: string, enum: [live, practice] }
        webullConfigured: { type: boolean, description: Live credentials stored }
        webullPracticeConfigured: { type: boolean, description: Practice credentials stored }
    WebullCredentialsInput:
      type: object
      required: [appKey, appSecret, accountId]
      properties:
        appKey: { type: string }
        appSecret: { type: string }
        accountId: { type: string }
        environment: { type: string, enum: [live, practice], default: live }
    Quote:
      type: object
      properties:
        symbol: { type: string }
        bid: { type: number }
        ask: { type: number }
        last: { type: number }
        bidSize: { type: integer }
        askSize: { type: integer }
        volume: { type: integer }
        timestamp: { type: string, format: date-time }
    Candle:
      type: object
      properties:
        time: { type: string, format: date-time }
        open: { type: number }
        high: { type: number }
        low: { type: number }
        close: { type: number }
        volume: { type: integer }
    OptionContract:
      type: object
      properties:
        symbol: { type: string }
        underlying: { type: string }
        expiration: { type: string, format: date }
        strike: { type: number }
        optionType: { type: string, enum: [call, put] }
        bid: { type: number }
        ask: { type: number }
        last: { type: number }
    OptionsChain:
      type: object
      properties:
        underlying: { type: string }
        underlyingPrice: { type: number }
        expirations: { type: array, items: { type: string, format: date } }
        contracts: { type: array, items: { $ref: '#/components/schemas/OptionContract' } }
    OrderRequest:
      type: object
      required: [underlying, assetClass, side, quantity, orderType, selection]
      properties:
        underlying: { type: string }
        assetClass: { type: string, enum: [option] }
        side: { type: string, enum: [buy, sell] }
        quantity: { type: integer, minimum: 1 }
        orderType: { type: string, enum: [mid, market] }
        selection:
          type: object
          required: [mode]
          properties:
            mode: { type: string, enum: [auto_otm, explicit] }
            optionType: { type: string, enum: [call, put] }
            expiration: { type: string, format: date }
            strike: { type: number }
    OrderPreview:
      type: object
      properties:
        resolved:
          type: object
          properties:
            contractSymbol: { type: string }
            price: { type: number }
            estBuyingPower: { type: number }
        warnings: { type: array, items: { type: string } }
    OrderResult:
      type: object
      properties:
        orderId: { type: string }
        status: { type: string, enum: [submitted, filled, partially_filled, cancelled, rejected] }
        contractSymbol: { type: string }
        side: { type: string }
        quantity: { type: integer }
        orderType: { type: string }
        limitPrice: { type: number }
        filledPrice: { type: number }
        filledQuantity: { type: number, description: Broker-reported executed quantity (partial fills); absent when the broker gives only the order quantity }
        timestamp: { type: string, format: date-time }
    TradeHistoryEntry:
      allOf:
        - $ref: '#/components/schemas/OrderResult'
        - type: object
          properties:
            realizedPnl: { type: [number, 'null'], description: Realized P/L for closing fills; null for opening fills and non-fills }
    TradeHistory:
      type: object
      properties:
        entries: { type: array, items: { $ref: '#/components/schemas/TradeHistoryEntry' }, description: Newest first }
        totalRealizedPnl: { type: number }
    Position:
      type: object
      properties:
        symbol: { type: string }
        assetClass: { type: string }
        quantity: { type: integer }
        avgPrice: { type: number }
        markPrice: { type: number }
        unrealizedPnl: { type: number }
        multiplier: { type: number, description: Contract multiplier (options: 100) for client-side live P/L }
    GexPremiumLevel:
      type: object
      properties:
        strike: { type: number }
        totalPremium: { type: number }
        callPremium: { type: number }
        putPremium: { type: number }
        callOi: { type: integer }
        putOi: { type: integer }
    GexLevels:
      type: object
      properties:
        symbol: { type: string }
        expiration: { type: string, format: date }
        isZeroDte: { type: boolean }
        spot: { type: number }
        asOf: { type: string, format: date-time }
        stale: { type: boolean, description: Served from the last-good cache after a Tradier failure }
        netGex: { type: number }
        netDex: { type: number }
        gammaFlip: { type: [number, 'null'], description: Spot where net GEX crosses zero }
        callWall: { type: [number, 'null'] }
        putWall: { type: [number, 'null'] }
        magnet: { type: [number, 'null'], description: Highest total-OI strike (0DTE pin target) }
        topPremium: { type: array, items: { $ref: '#/components/schemas/GexPremiumLevel' } }
